{
  "buildCommand": "unzip zentropk_new.zip && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
